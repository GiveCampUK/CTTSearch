@using System.Web.Mvc.Html
@using SearchParty.Core.Models
@model Resource

@{
    ViewBag.Title = "title";
}

<h2>Test Form for adding resource. Delete when no longer needed</h2>

<form action="@Url.Action("Update", "Resource")" method="POST">
@Html.HiddenFor(r => r.Id)
@Html.HiddenFor(r => r.Tags)
@Html.HiddenFor(r => r.Title)
@Html.HiddenFor(r => r.ShortDescription)
@Html.HiddenFor(r => r.Uri)
@Html.HiddenFor(r => r.ResourceType)
@Html.HiddenFor(r => r.LongDescription)
<input type="submit" />
</form>